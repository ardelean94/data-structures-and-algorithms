/******In order to EXECUTE - Add the following lines in Program.cs > Main method:******/

string input = Console.ReadLine().Trim();
Console.WriteLine(Palindrome.IsPalindrome(input) ? "Palindrome" : "Not Palindrome"); 